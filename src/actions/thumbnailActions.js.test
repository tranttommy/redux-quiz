import { retrievePokemons, RETRIEVE_POKEMONS } from './thumbnailActions';

describe('thumbnail actions', () => {
  it('creates an action object', () => {
    expect(retrievePokemons()).toEqual({
      type: RETRIEVE_POKEMONS,
      payload: expect.any(Promise)
    });
  });
});
